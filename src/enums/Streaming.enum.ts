export enum WhoCanChat {
    REGISTERED_AND_VERIFIED_EMAILS = 'Registered users with verified emails(recommended)',
    USERS_WITH_TK_AND_SUBS = 'Users who have bought tokens or subscriptions',
}

export enum KingOfTheRoom {
    FROM_O_TK = 'From 0 tokens(recommended)',
    FROM_1O_TK = 'From 10 tokens(recommended)',
    FROM_5O_TK = 'From 50 tokens(recommended)',
    FROM_10O_TK = 'From 100 tokens(recommended)',
    FROM_50O_TK = 'From 500 tokens(recommended)',
    CUSTOM_AMOUNT = 'Custom amount',
}
