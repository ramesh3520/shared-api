export enum StripScoreFactor {
    REVENUE_PER_HOUR = 'revenue per hour',
    PROFILE_COMPLETENESS = 'profile completeness',
    STREAM_QUALITY = 'stream quality',
    MICROPHONE_USAGE = 'microphone usage',
    BACKGROUND_MEDIA = 'background media',
    PUBLIC_CONTENT = 'public content',
    BROADCAST_DETAILS = 'broadcast details',
    TIP_MENU = 'tip menu',
    PRIVATE_SHOW_RATINGS = 'private show ratings',
}
